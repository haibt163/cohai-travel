# Catalog photography

Seed rows point at `/media/*.jpg`.

1. Editorial SVG plates in this folder cover first paint so a standalone checkout is not blank.
2. `npm run media` downloads Unsplash stills onto the same `.jpg` names.

Replace both with licensed operator photography in Phase 2. Do not commit secrets or scraped WordPress uploads.
